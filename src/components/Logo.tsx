export default function Logo() {
  return (
    <div className="inline-flex items-center gap-3 font-bold text-[var(--eq-midnight)]">
      <img src="/logo.png" alt="Logo" className="h-16 w-auto object-contain" />
    </div>
  );
}
